# Singleton là có nhiệm vụ chỉ tạo 1 phiên bản của một lớp duy nhất và cung cấp điểm truy cập cho cả ứng dụng
###    => Một singleton cung cấp một module duy nhất (Instance) có thể truy cập trên tất cả ứng dụng mà không cần khởi tạo lại
# ==================
# ƯU ĐIỂM:
## + Dễ thực hiện, cho các app design pattern và phổ biển hiện này (mọi level đều có thể sử dụng)

# ==================
## + Window có task manager (chỉ mở dc 1tab duy nhất)
## + Kết nối CSDL
